#include "oled.h"
#include <stdio.h>

// Stub for initializing the OLED display
void oled_init(void)
{
    // Replace this stub with initialization code (e.g., I2C setup, SSD1306 driver initialization)
    printf("OLED display initialized (stub).\n");
}

// Stub for displaying text on the OLED screen
void oled_display_text(const char *line)
{
    // Replace this stub with code to write text to the OLED screen.
    printf("OLED Display: %s\n", line);
}
